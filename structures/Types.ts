export const discordId = {
    type: String,
    required: true,
    unique: true
}

export const reqString = {
    type: String,
    required: true
}

export const reqNum = {
    type: Number,
    required: true
}

export const reqBool = {
    type: Boolean,
    required: true
}